{% extends 'base.html.twig' %}

{% block body %}
<section id="top" class="relative w-full h-[85vh] min-h-[650px] flex items-center overflow-hidden bg-slate-950">
    
    <div id="hero-bg-default" class="absolute inset-0 z-0">
        <video autoplay muted loop playsinline class="w-full h-full object-cover select-none pointer-events-none">
            <source src="/videos/SAMPLEHERO2.mp4" type="video/mp4">
        </video>
        <div class="absolute inset-0 bg-gradient-to-r from-black/90 via-black/60 to-black/20 md:to-transparent"></div>
    </div>

    <div id="hero-bg-overlay" class="absolute inset-0 z-[1] transition-opacity duration-700 ease-in-out opacity-0 pointer-events-none">
        <img id="hero-bg-overlay-blur" src="" alt="" class="absolute inset-0 w-full h-full object-cover opacity-100">
        <img id="hero-bg-overlay-img" src="" alt="" class="relative z-10 w-full h-full object-contain">
    </div>

    <div id="hero-content" class="relative z-30 w-full max-w-7xl mx-auto px-6 sm:px-4 lg:px-16 flex flex-col justify-center items-start text-left">
        
        <span class="font-roboto text-white font-semibold tracking-[0.2em] uppercase text-xs sm:text-sm mb-4 bg-feu-green/5 px-3 py-1.5 rounded-md inline-block backdrop-blur-sm border border-white/20">
            Welcome to The School of Innovation
        </span>
        
        <h1 class="font-poppins text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold text-white mb-6 leading-[1.4] tracking-wide max-w-4xl" style="text-shadow: 0 4px 16px rgba(0, 0, 0, 0.45), 0 2px 4px rgba(0, 0, 0, 0.3);">
            Shape Your Future at <span class="text-feu-yellow font-black" style="text-shadow: 0 4px 12px rgba(240, 179, 35, 0.25), 0 2px 4px rgba(0, 0, 0, 0.4);"><br>FEU Tech</span>
        </h1>
        
        <p class="font-roboto text-base md:text-lg lg:text-l text-white mb-10 font-normal leading-relaxed max-w-2xl tracking-wide drop-shadow-md">
            FEU Tech is an AI Campus and an internationally ranked institution recognized for global excellence in Engineering, Computer Science, I.T., Multimedia Arts and Research. It is committed to producing graduates who are industry-ready and globally competitive from day one.
        </p>
        
        <div class="flex flex-wrap gap-4 w-full sm:w-auto">
            <a href="{{ path('app_ccsma') }}" class="w-full sm:w-auto text-center px-8 py-4 bg-white/5 backdrop-blur-md text-feu-yellow font-bold rounded-lg border border-white/20 hover:bg-white/10 hover:border-white/45 active:scale-98 transition-all text-sm tracking-wider font-poppins uppercase shadow-lg">
                CCSMA
            </a>
            
            <a href="/freshmen" class="w-full sm:w-auto text-center px-10 py-4 bg-feu-yellow text-feu-green font-black rounded-lg shadow-2xl shadow-feu-yellow/20 hover:bg-yellow-400 hover:shadow-feu-yellow/45 hover:-translate-y-0.5 active:translate-y-0 transition-all text-sm tracking-wider font-poppins uppercase">
                Apply Now
            </a>
            
            <a href="{{ path('app_coe') }}" class="w-full sm:w-auto text-center px-8 py-4 bg-white/5 backdrop-blur-md text-feu-yellow font-bold rounded-lg border border-white/20 hover:bg-white/10 hover:border-white/45 active:scale-98 transition-all text-sm tracking-wider font-poppins uppercase shadow-lg">
                COE
            </a>
        </div>
        
    </div>
</section>

<script>
function initCarousel() {
    if (window.homeCarouselInitialized) return;

    const trackWrapper = document.getElementById('carousel-track-wrapper');
    const track = document.getElementById('carousel-track');
    const overlay = document.getElementById('hero-bg-overlay');
    const overlayImg = document.getElementById('hero-bg-overlay-img');
    const overlayBlur = document.getElementById('hero-bg-overlay-blur');
    const heroContent = document.getElementById('hero-content');

    const leftArrow = document.getElementById('carousel-left-arrow')?.firstElementChild;
    const rightArrow = document.getElementById('carousel-right-arrow')?.firstElementChild;

    if (!track || !overlay || !overlayImg || !trackWrapper) return;

    window.homeCarouselInitialized = true;

    const cards = track.querySelectorAll('.carousel-card');
    if (!cards.length) return;

    let scrollPos = 0;
    let baseSpeed = 0.9;
    let isPaused = false;
    let isFastForward = false;
    let isFastRewind = false;
    let resumeTimeout = null;
    let animFrameId = null;

    function getOneSetWidth() {
        const cardCount = cards.length / 3;
        let w = 0;

        for (let i = 0; i < cardCount; i++) {
            w += cards[i].offsetWidth + 24;
        }

        return w;
    }

    /*
    function setupTamTamPersistent() {

        // Remove old tamtams before creating new ones
        track.querySelectorAll('.tamtam-mascot').forEach(el => el.remove());

        const uniqueCardCount = cards.length / 3;
        const uniqueTitles = [];

        for (let i = 0; i < uniqueCardCount; i++) {
            const titleEl = cards[i].querySelector('h4');

            if (titleEl) {
                uniqueTitles.push(titleEl.textContent.trim());
            }
        }

        if (!uniqueTitles.length) return;

        const chosenTitle =
            uniqueTitles[Math.floor(Math.random() * uniqueTitles.length)];

        cards.forEach(card => {
            const titleEl = card.querySelector('h4');

            if (
                titleEl &&
                titleEl.textContent.trim() === chosenTitle
            ) {
                const tamtam = document.createElement('img');

                tamtam.src = '/images/Home_Carousel/TamtamSit.png';

                tamtam.className =
                    'tamtam-mascot absolute -top-20 left-1/2 -translate-x-1/2 w-24 h-24 object-contain z-[100] pointer-events-none opacity-100';

                card.appendChild(tamtam);
            }
        });
    }

    setupTamTamPersistent();
    */
    
    function updateTrackPosition() {
        const oneSet = getOneSetWidth();

        if (scrollPos >= oneSet) {
            scrollPos -= oneSet;
        } else if (scrollPos < 0) {
            scrollPos += oneSet;
        }

        track.style.transform = `translateX(-${scrollPos}px)`;
    }

    function animate() {
        let activeSpeed = 0;

        if (isFastForward) {
            activeSpeed = 4;
        } else if (isFastRewind) {
            activeSpeed = -4;
        } else if (!isPaused) {
            activeSpeed = baseSpeed;
        }

        if (activeSpeed !== 0) {
            scrollPos += activeSpeed;
            updateTrackPosition();
        }

        animFrameId = requestAnimationFrame(animate);
    }

    animFrameId = requestAnimationFrame(animate);

    trackWrapper.addEventListener('mouseenter', () => {
        if (resumeTimeout) {
            clearTimeout(resumeTimeout);
            resumeTimeout = null;
        }

        isPaused = true;
    });

    trackWrapper.addEventListener('mouseleave', () => {
        isFastForward = false;
        isFastRewind = false;

        resumeTimeout = setTimeout(() => {
            isPaused = false;

            overlay.classList.remove('opacity-100');
            overlay.classList.add('opacity-0');

            heroContent.style.opacity = '1';

            resumeTimeout = null;
        }, 1000);
    });

    cards.forEach(card => {
        card.addEventListener('mouseenter', () => {
            const imgSrc = card.getAttribute('data-carousel-img');
            const heroBg = card.getAttribute('data-hero-bg');

            if (imgSrc) {
                overlayImg.src = imgSrc;

                if (overlayBlur) {
                    overlayBlur.src = heroBg;
                }

                overlay.classList.remove('opacity-0');
                overlay.classList.add('opacity-100');

                heroContent.style.opacity = '0';
                heroContent.style.transition = 'opacity 0.5s ease';
            }
        });
    });

    if (leftArrow) {
        leftArrow.addEventListener('mouseenter', () => {
            isFastRewind = true;
        });

        leftArrow.addEventListener('mouseleave', () => {
            isFastRewind = false;
        });

        leftArrow.addEventListener('click', e => {
            e.stopPropagation();

            scrollPos -= 304;
            updateTrackPosition();
        });
    }

    if (rightArrow) {
        rightArrow.addEventListener('mouseenter', () => {
            isFastForward = true;
        });

        rightArrow.addEventListener('mouseleave', () => {
            isFastForward = false;
        });

        rightArrow.addEventListener('click', e => {
            e.stopPropagation();

            scrollPos += 304;
            updateTrackPosition();
        });
    }

    document.addEventListener(
        'turbo:before-render',
        function cleanup() {

            if (animFrameId) {
                cancelAnimationFrame(animFrameId);
            }

            if (resumeTimeout) {
                clearTimeout(resumeTimeout);
            }

            // Remove dynamically-added tamtams before Turbo caches page
            track.querySelectorAll('.tamtam-mascot').forEach(el => el.remove());

            window.homeCarouselInitialized = false;

            document.removeEventListener(
                'turbo:before-render',
                cleanup
            );
        },
        { once: true }
    );
}

document.addEventListener('turbo:load', initCarousel);
document.addEventListener('DOMContentLoaded', initCarousel);

if (
    document.readyState === 'complete' ||
    document.readyState === 'interactive'
) {
    initCarousel();
}
</script>


<section id="courses" class="py-24 relative overflow-hidden border-t-4 border-feu-yellow scroll-animate">
    <div class="absolute inset-0 z-0">
        <img src="/images/FEU-TECHBLD.jpg" alt="Apply Background" class="w-full h-full object-cover">
        <div class="absolute inset-0 bg-[#008348]/90"></div>
    </div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
            <div class="flex flex-col gap-6">
                <div class="bg-[#129658] rounded-3xl p-8 shadow-lg flex flex-col">
                    <h3 class="text-2xl font-extrabold text-white mb-4 uppercase tracking-wide">College of Engineering</h3>
                    <p class="text-white/90 mb-8 leading-relaxed pr-4">Build the future with fully-accredited civil, electronics, mechanical, and computer engineering programs. Take the first step towards a successful career.</p>
                    <div class="mt-auto">
                        <a href="/academics/coe" class="inline-block px-8 py-3 bg-[#f0b323] text-slate-900 font-bold rounded-full hover:bg-yellow-400 transition-colors shadow-md">
                            Apply Now!
                        </a>
                    </div>
                </div>

                <div class="bg-[#129658] rounded-3xl p-8 shadow-lg flex flex-col">
                    <h3 class="text-2xl font-extrabold text-white mb-4 uppercase tracking-wide">College of Computer Studies & Multimedia Arts</h3>
                    <p class="text-white/90 mb-8 leading-relaxed pr-4">Dive deep into software engineering, artificial intelligence, and premier creative multimedia programs. Lead the digital revolution.</p>
                     <div class="mt-auto">
                         <a href="/academics/ccsma" class="inline-block px-8 py-3 bg-white text-[#008348] font-bold rounded-full hover:bg-slate-50 transition-colors shadow-md">
                            Apply Now!
                        </a>
                     </div>
                </div>
            </div>

            <div class="bg-white rounded-[2rem] p-8 md:p-10 shadow-2xl flex flex-col justify-center">
                <div class="accordion-item border-b border-slate-100 pb-6 mb-6">
                    <button class="accordion-btn w-full flex justify-between items-center text-left group focus:outline-none">
                        <span class="text-[1.35rem] font-bold text-slate-800">About FEU Tech Experience</span>
                        <div class="accordion-icon w-8 h-8 rounded-full bg-slate-50 text-slate-400 flex items-center justify-center flex-shrink-0">
                            <svg class="w-5 h-5 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </div>
                    </button>

                    <div class="accordion-content mt-6 text-slate-500 leading-relaxed text-[0.95rem] pr-2">
                        At FEU Tech, we're more than a school — we're a launchpad for your future. We push boundaries and shape industry leaders through fully-accredited engineering, computer science, and multimedia programs. Here, you don't just learn — you build, innovate, and lead.
                    </div>
                </div>

                <div class="accordion-item border-b border-slate-100 pb-6 mb-6">
                    <button class="accordion-btn w-full flex justify-between items-center text-left group focus:outline-none">
                        <span class="text-[1.35rem] font-bold text-slate-800">Campus Life</span>
                        <div class="accordion-icon w-8 h-8 rounded-full bg-slate-50 text-slate-400 flex items-center justify-center flex-shrink-0">
                            <svg class="w-5 h-5 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </div>
                    </button>

                    <div class="accordion-content hidden mt-6 text-slate-500 leading-relaxed text-[0.95rem] pr-2">
                        Lorem ipsum 
                    </div>
                </div>

                <div class="accordion-item border-b border-slate-100 pb-6 mb-6">
                    <button class="accordion-btn w-full flex justify-between items-center text-left group focus:outline-none">
                        <span class="text-[1.35rem] font-bold text-slate-800">Student Organizations</span>
                        <div class="accordion-icon w-8 h-8 rounded-full bg-slate-50 text-slate-400 flex items-center justify-center flex-shrink-0">
                            <svg class="w-5 h-5 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </div>
                    </button>

                    <div class="accordion-content hidden mt-6 text-slate-500 leading-relaxed text-[0.95rem] pr-2">
                        Lorem ipsum
                     </div>
                </div>

                <div class="accordion-item pt-2">
                    <button class="accordion-btn w-full flex justify-between items-center text-left group focus:outline-none">
                        <span class="text-[1.35rem] font-bold text-slate-800">Regular Events</span>
                        <div class="accordion-icon w-8 h-8 rounded-full bg-slate-50 text-slate-400 flex items-center justify-center flex-shrink-0">
                            <svg class="w-5 h-5 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </div>
                    </button>

                    <div class="accordion-content hidden mt-6 text-slate-500 leading-relaxed text-[0.95rem] pr-2">
                        Lorem ipsum
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

<section class="relative py-28 overflow-hidden bg-gradient-to-br from-white via-yellow-50 to-emerald-50 border-t-4 border-feu-yellow scroll-animate">
    <div class="absolute top-0 left-0 w-96 h-96 bg-feu-yellow/20 blur-3xl rounded-full"></div>
    <div class="absolute bottom-0 right-0 w-96 h-96 bg-feu-green/20 blur-3xl rounded-full"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-emerald-100/10 blur-3xl rounded-full pointer-events-none"></div>

    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col gap-20 items-center w-full">
            
            <div class="w-full max-w-5xl text-center transform transition-all duration-700 hover:translate-y-[-4px]">
                <h2 class="text-2xl md:text-3xl font-extrabold text-slate-800 tracking-tight mb-4 flex items-center justify-center gap-3">
                    <span class="w-8 h-1 bg-feu-yellow rounded-full"></span>
                    Student Magazines
                    <span class="w-8 h-1 bg-feu-yellow rounded-full"></span>
                </h2>

                <div class="relative h-[580px] flex items-center justify-center overflow-visible select-none [perspective:1000px]" id="magazineCarousel">
                    <div class="absolute bottom-4 left-1/2 -translate-x-1/2 w-[85%] h-16 bg-black/15 blur-[50px] rounded-full pointer-events-none"></div>
                    
                    <a id="magLeftLink" target="_blank" rel="noopener noreferrer" class="absolute left-2 md:left-6 top-20 w-[220px] md:w-[300px] z-10 block transform -rotate-6 hover:-rotate-12 transition-all duration-500 hover:z-20">
                        <img id="magLeft"
                            src="/images/magazine/issue1.png"
                            class="w-full rounded-lg shadow-[0_15px_35px_rgba(0,0,0,0.2)] hover:shadow-[0_25px_50px_rgba(0,0,0,0.35)] transition-all duration-500">
                    </a>

                    <div class="absolute z-30 w-[300px] md:w-[410px] transform hover:scale-[1.03] transition-all duration-500 group/center">
                        <a id="magMainLink" target="_blank" rel="noopener noreferrer" class="block relative">
                            <img id="magMain"
                                src="/images/magazine/issue3.png"
                                class="w-full rounded-xl shadow-[0_35px_70px_rgba(0,0,0,0.35)] border border-white/20 transition-all duration-500">
                            <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent opacity-0 group-hover/center:opacity-100 transition-opacity duration-300 rounded-xl pointer-events-none"></div>
                        </a>

                        <button id="prevMag"
                            class="absolute left-[-24px] md:left-[-32px] top-1/2 -translate-y-1/2 
                            w-14 h-14 rounded-full bg-white text-slate-800 border border-slate-100
                            shadow-xl flex items-center justify-center font-bold text-xl
                            hover:bg-feu-yellow hover:text-slate-900 active:scale-95 transition-all z-40">
                            &lt;
                        </button>

                        <button id="nextMag"
                            class="absolute right-[-24px] md:right-[-32px] top-1/2 -translate-y-1/2 
                            w-14 h-14 rounded-full bg-white text-slate-800 border border-slate-100
                            shadow-xl flex items-center justify-center font-bold text-xl
                            hover:bg-feu-yellow hover:text-slate-900 active:scale-95 transition-all z-40">
                            &gt;
                        </button>
                    </div>

                    <a id="magRightLink" target="_blank" rel="noopener noreferrer" class="absolute right-2 md:right-6 top-16 w-[240px] md:w-[330px] z-20 block transform rotate-6 hover:rotate-12 transition-all duration-500 hover:z-20">
                        <img id="magRight"
                            src="/images/magazine/issue2.png"
                            class="w-full rounded-lg shadow-[0_20px_40px_rgba(0,0,0,0.22)] hover:shadow-[0_25px_50px_rgba(0,0,0,0.35)] transition-all duration-500">
                    </a>
                </div>
            </div>

            <div class="w-full max-w-5xl h-px bg-gradient-to-r from-transparent via-slate-200 to-transparent my-2"></div>

            <div class="w-full max-w-6xl text-left">
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 items-start">
                    
                    <div class="lg:col-span-2 flex flex-col gap-4">
                        <div class="group/player relative rounded-xl overflow-hidden bg-black aspect-video shadow-lg border border-slate-200">
                            <a id="vidMainLink" href="" target="_blank" rel="noopener noreferrer" class="block w-full h-full relative">
                                
                                <div class="absolute inset-0 flex items-center justify-center z-20">
                                    <div class="w-16 h-16 rounded-full bg-red-600 text-white shadow-xl flex items-center justify-center transform scale-100 group-hover/player:scale-110 transition-transform duration-200">
                                        <svg class="w-7 h-7 ml-1" fill="currentColor" viewBox="0 0 24 24">
                                            <path d="M8 5v14l11-7z"/>
                                        </svg>
                                    </div>
                                </div>

                                <img id="vidMainThumb" src="" alt="Active Video Player Thumbnail" class="w-full h-full object-cover">
                            </a>
                        </div>
                        
                        <div class="px-1">
                            <h3 id="vidMainTitle" class="text-xl font-bold text-slate-900 leading-snug">Loading Active Content Title...</h3>
                            <div class="flex items-center gap-3 mt-3 pb-4 border-b border-slate-200">
                                <div class="w-10 h-10 rounded-full overflow-hidden flex items-center justify-center p-1.5 bg-slate-50 border border-slate-200">
                                    <img src="/images/Tech_Logo.png" alt="FEU Tech" class="w-full h-full object-contain">
                                </div>
                                <div class="flex flex-col">
                                    <span class="font-bold text-sm text-slate-800">TAMbayan TV</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="flex flex-col gap-3">
                        <div class="flex flex-col gap-3 max-h-[460px] overflow-y-auto pr-1" id="youtubePlaylistFeed"></div>
                    </div>

                </div>
            </div>

        </div>
    </div>
</section>

<section id="meetings" class="py-12 bg-slate-50 scroll-animate relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        <div class="flex flex-col md:flex-row justify-between items-end mb-12">
            <div>
                <span class="text-feu-green font-bold tracking-wider uppercase text-sm mb-2 block">
                    Events Conducted
                </span>
                <h2 class="text-4xl font-extrabold text-slate-800">
                    In the news
                </h2>
            </div>

            <a href="#"
                class="mt-4 md:mt-0 px-6 py-3 border-2 border-feu-green text-feu-green font-bold rounded-lg hover:bg-feu-green hover:text-white transition-all shadow-sm">
                View All Events
            </a>
        </div>

        <div id="eventsCarousel" class="flex overflow-x-auto gap-8 no-scrollbar snap-x snap-mandatory pb-8 scroll-smooth w-full">

            {% set events = [
                {
                    path: 'event_one',
                    image: '/images/carousels/MAY5.jfif',
                    alt: 'National Tech Symposium',
                    badge: 'SEMINARS',
                    date: 'May 5, 2026',
                    title: 'Shaping the Future of Education: Utah Valley University, FEU Tech, Unite for AI Innovation Workshop',
                    description: 'Join industry leaders as they discuss the future of AI and Machine Learning innovations.'
                },
                {
                    path: 'event_two',
                    image: '/images/carousels/TARA.jpg',
                    alt: 'Research Expo',
                    badge: 'EXHIBITIONS',
                    date: 'Nov 24, 2026',
                    title: '#TaraNaSaPIYU 2026: Welcoming the Next Generation of Innovators at FEU Tech',
                    description: 'Showcasing the brightest capstone projects from our graduating engineering students.'
                },
                {
                    path: 'event_three',
                    image: '/images/carousels/FTIC.jpg',
                    alt: 'Hackathon',
                    badge: 'COMPETITIONS',
                    date: 'April 24, 2026',
                    title: 'FTIC Ramps Up the Culture of Innovation at FEU Tech with a Campus-Wide Symposium',
                    description: 'A 48-hour coding marathon to solve real-world problems. Open to all students.'
                }
            ] %}

            {% for event in events %}
                <div class="w-full md:w-[calc(50%-1rem)] min-w-full md:min-w-[calc(50%-1rem)] snap-start flex-shrink-0 bg-white rounded-3xl overflow-hidden shadow-xl border border-slate-100 flex flex-col hover:shadow-2xl transition-all duration-300">
                    <a href="{{ path(event.path) }}" class="block group">
                        
                        <div class="relative h-80 overflow-hidden bg-slate-100">
                            <img src="{{ event.image }}"
                                alt="{{ event.alt }}"
                                class="w-full h-full object-contain group-hover:scale-105 transition-transform duration-700">

                            <div class="absolute inset-0 bg-gradient-to-t from-black/40 to-transparent"></div>

                            <div class="absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
                                <div class="bg-white/20 backdrop-blur-md p-4 rounded-full transform scale-75 group-hover:scale-100 transition-all duration-300">
                                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.8" stroke="white" class="w-8 h-8">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12s3.75-7.5 9.75-7.5S21.75 12 21.75 12s-3.75 7.5-9.75 7.5S2.25 12 2.25 12z" />
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    </svg>
                                </div>
                            </div>
                        </div>

                        <div class="p-6 flex flex-col flex-grow">
                            <div class="mb-4">
                                <span class="px-3 py-1.5 text-xs font-bold tracking-wider text-white bg-[#004d33] rounded-full">
                                    {{ event.badge }}
                                </span>
                            </div>

                            <div class="flex items-center gap-2 mb-4 text-slate-500 font-medium text-sm">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                </svg>
                                <span>{{ event.date }}</span>
                            </div>

                            <h3 class="text-2xl font-bold text-slate-800 mb-3 group-hover:text-feu-green transition-colors line-clamp-2">
                                {{ event.title }}
                            </h3>

                            <p class="text-slate-600 leading-relaxed line-clamp-2">
                                {{ event.description }}
                            </p>
                        </div>

                    </a>
                </div>
            {% endfor %}

        </div>

        <div class="flex justify-end gap-3 mt-4">
            <button id="prevCarouselBtn" class="w-12 h-12 flex items-center justify-center rounded-full border border-feu-green text-feu-green hover:bg-feu-green hover:text-white transition-all">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                </svg>
            </button>
            <button id="nextCarouselBtn" class="relative w-12 h-12 flex items-center justify-center rounded-full border border-feu-green text-feu-green hover:bg-feu-green hover:text-white transition-all overflow-hidden">
                <div id="carouselLoader" class="absolute left-0 bottom-0 top-0 bg-feu-green/20 w-0 transition-all duration-[5000ms] linear pointer-events-none"></div>
                <svg class="w-5 h-5 relative z-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                </svg>
            </button>
        </div>

    </div>
</section>

<script>
document.addEventListener('turbo:load', () => {
    const carousel = document.getElementById('eventsCarousel');
    const prevBtn = document.getElementById('prevCarouselBtn');
    const nextBtn = document.getElementById('nextCarouselBtn');
    const loader = document.getElementById('carouselLoader');
    
    if (!carousel || !prevBtn || !nextBtn || !loader) return;

    const AUTO_PLAY_TIME = 5000; 
    let autoPlayInterval;

    function getScrollAmount() {
        const firstCard = carousel.firstElementChild;
        return firstCard ? firstCard.offsetWidth + 32 : 400; 
    }

    function resetLoaderAnimation() {
        loader.style.transition = 'none';
        loader.style.width = '0%';
        void loader.offsetWidth; 
        loader.style.transition = `width ${AUTO_PLAY_TIME}ms linear`;
        loader.style.width = '100%';
    }

    function handleNextScroll() {
        const maxScroll = carousel.scrollWidth - carousel.clientWidth;
        if (Math.ceil(carousel.scrollLeft) >= maxScroll - 10) {
            carousel.scrollTo({ left: 0, behavior: 'smooth' });
        } else {
            carousel.scrollBy({ left: getScrollAmount(), behavior: 'smooth' });
        }
        resetLoaderAnimation();
    }

    function handlePrevScroll() {
        if (carousel.scrollLeft <= 10) {
            carousel.scrollTo({ left: carousel.scrollWidth, behavior: 'smooth' });
        } else {
            carousel.scrollBy({ left: -getScrollAmount(), behavior: 'smooth' });
        }
        resetLoaderAnimation();
    }

    function startAutoPlay() {
        resetLoaderAnimation();
        autoPlayInterval = setInterval(handleNextScroll, AUTO_PLAY_TIME);
    }

    function stopAutoPlay() {
        clearInterval(autoPlayInterval);
        loader.style.transition = 'none';
        loader.style.width = '0%';
    }

    nextBtn.addEventListener('click', () => {
        stopAutoPlay();
        handleNextScroll();
        startAutoPlay();
    });

    prevBtn.addEventListener('click', () => {
        stopAutoPlay();
        handlePrevScroll();
        startAutoPlay();
    });

    carousel.addEventListener('mouseenter', stopAutoPlay);
    carousel.addEventListener('mouseleave', startAutoPlay);

    startAutoPlay();
});
</script>

<script>
const magazines = [
    {
        img: "/images/magazine/issue1.png",
        link: "https://drive.google.com/file/d/1Nvc5Qvx9kIQOijV6fzbx6O6VC1t5M4LD/view"
    },
    {
        img: "/images/magazine/issue3.png",
        link: "https://heyzine.com/flip-book/fd6d3a5475.html#page/1"
    },
    {
        img: "/images/magazine/issue2.png",
        link: "https://drive.google.com/file/d/1rpQ8TCiGqPfzIsufRCltl_Y39OavyN9T/view"
    },
    {
        img: "/images/magazine/issue5.png",
        link: "https://heyzine.com/flip-book/574ef6415d.html"
    }
];

let magIndex = 1;

document.addEventListener('turbo:load', () => {
    const magLeft = document.getElementById("magLeft");
    const magMain = document.getElementById("magMain");
    const magRight = document.getElementById("magRight");
    const prevMagBtn = document.getElementById("prevMag");
    const nextMagBtn = document.getElementById("nextMag");

    if (!magLeft || !magMain || !magRight || !prevMagBtn || !nextMagBtn) return;

    prevMagBtn.addEventListener("click", () => {
        magIndex = (magIndex - 1 + magazines.length) % magazines.length;
        updateMag();
    });

    nextMagBtn.addEventListener("click", () => {
        magIndex = (magIndex + 1) % magazines.length;
        updateMag();
    });

    function updateMag() {
        const leftIndex = (magIndex - 1 + magazines.length) % magazines.length;
        const rightIndex = (magIndex + 1) % magazines.length;

        const left = magazines[leftIndex];
        const main = magazines[magIndex];
        const right = magazines[rightIndex];

        magMain.src = main.img;
        magLeft.src = left.img;
        magRight.src = right.img;

        document.getElementById("magMainLink").href = main.link;
        document.getElementById("magLeftLink").href = left.link;
        document.getElementById("magRightLink").href = right.link;
    }

    updateMag();
});
</script>

<script>
document.addEventListener('turbo:load', () => {
    const videos = [
        {
            id: "d0a2jThLgx4",
            title: "MILES PARAVERSE",
            link: "https://www.youtube.com/watch?v=d0a2jThLgx4"
        },
        {
            id: "n_LEKwGma6g",
            title: "EXPLORE THE FEU TECH CAMPUS!",
            link: "https://www.youtube.com/watch?v=n_LEKwGma6g&source_ve_path=MTc4NDI0"
        },
        {
            id: "N36B85B5xC8",
            title: "\"Hall of Greats\" Music Video",
            link: "https://www.youtube.com/watch?v=N36B85B5xC8"
        },
        {
            id: "ewMDmlMNboE",
            title: "3 Easy Steps for Freshmen Enrollment at FEU Tech",
            link: "https://www.youtube.com/watch?v=ewMDmlMNboE"
        }
    ];

    let activeVideoIndex = 0;

    if (!document.getElementById("youtubePlaylistFeed")) return;

    window.setActiveVideo = function(index) {
        activeVideoIndex = index;
        const currentVideo = videos[activeVideoIndex];

        document.getElementById("vidMainThumb").src = `https://img.youtube.com/vi/${currentVideo.id}/maxresdefault.jpg`;
        document.getElementById("vidMainTitle").textContent = currentVideo.title;
        document.getElementById("vidMainLink").href = currentVideo.link;

        renderPlaylistFeed();
    };

    function renderPlaylistFeed() {
        const feedContainer = document.getElementById("youtubePlaylistFeed");
        if (!feedContainer) return;
        feedContainer.innerHTML = ""; 

        videos.forEach((video, index) => {
            const isCurrentlyPlaying = index === activeVideoIndex;
            
            const itemHTML = `
                <button onclick="setActiveVideo(${index})" class="w-full text-left flex gap-3 p-1.5 rounded-xl transition-all duration-200 ${isCurrentlyPlaying ? 'bg-slate-100 border border-slate-200/80' : 'hover:bg-slate-50 border border-transparent'} focus:outline-none group/item">
                    <div class="w-32 aspect-video rounded-lg overflow-hidden bg-slate-900 flex-shrink-0 relative border border-slate-100 shadow-sm">
                        <img src="https://img.youtube.com/vi/${video.id}/mqdefault.jpg" class="w-full h-full object-cover">
                        ${isCurrentlyPlaying ? `
                            <div class="absolute inset-0 bg-black/45 flex items-center justify-center">
                                <span class="text-[9px] font-extrabold bg-black/80 text-[#f0b323] px-2 py-0.5 rounded tracking-wider border border-white/10">NOW PLAYING</span>
                            </div>
                        ` : ''}
                    </div>
                    <div class="flex flex-col justify-start pt-0.5 overflow-hidden">
                        <h4 class="text-xs font-bold text-slate-800 line-clamp-2 leading-tight group-hover/item:text-slate-950">${video.title}</h4>
                        <span class="text-[10px] text-slate-500 mt-1 font-medium">Tambbayan Tv</span>
                    </div>
                </button>
            `;
            feedContainer.insertAdjacentHTML('beforeend', itemHTML);
        });
    }

    setActiveVideo(0);
});
</script>

<script>
document.addEventListener('turbo:load', () => {
    const accordions = document.querySelectorAll('.accordion-item');

    accordions.forEach(item => {
        const button = item.querySelector('.accordion-btn');
        const content = item.querySelector('.accordion-content');
        const icon = item.querySelector('svg');

        if (button) {
            button.addEventListener('click', () => {
                accordions.forEach(otherItem => {
                    if (otherItem !== item) {
                        otherItem.querySelector('.accordion-content')?.classList.add('hidden');
                        otherItem.querySelector('svg')?.classList.remove('rotate-180');
                    }
                });

                content.classList.toggle('hidden');
                icon.classList.toggle('rotate-180');
            });
        }
    });
});
</script>

<style>
.no-scrollbar::-webkit-scrollbar {
    display: none;
}
.no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
}
</style>
{% endblock %}