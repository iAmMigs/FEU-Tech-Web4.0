{% extends 'base.html.twig' %}

{% block title %}{{ content.metaTitle|default('iCARE - FEU Institute of Technology') }}{% endblock %}

{% block meta_description %}{{ content.metaDescription|default('') }}{% endblock %}
{% block meta_keywords %}{{ content.metaKeywords|default('') }}{% endblock %}

{% block body %}
<style>
    :root {
        --feu-green: #008348;
        --feu-yellow: #febe10;
    }
</style>

<!-- Hero Section -->
<section id="top" class="relative w-full h-[50vh] min-h-[400px] flex items-center justify-center overflow-hidden bg-slate-900">
    <div class="absolute inset-0 z-0">
        {% if content and content.heroImage %}
            <img src="/images/studentservice/{{ content.heroImage }}" class="w-full h-full object-cover opacity-40" alt="Hero Banner">
        {% else %}
            <!-- Fallback design setup -->
            <div class="w-full h-full bg-slate-800 absolute"></div>
        {% endif %}
        <img src="/images/Tamtam/Reading_Book_Tamby.png" class="absolute w-[800px] opacity-10 mix-blend-screen right-[-100px] top-[-50px]" alt="">
        <div class="absolute inset-0 bg-gradient-to-t from-slate-900 via-transparent to-slate-900/60"></div>
    </div>

    <div class="relative z-10 text-center px-5 max-w-4xl mx-auto transform translate-y-4 animate-fade-in-up">
        <span class="text-[#febe10] font-bold tracking-[0.3em] uppercase text-xs mb-4 block">
            Student Journey
        </span>
        <h1 class="text-5xl md:text-7xl font-black text-white leading-tight uppercase tracking-tighter">
            {{ content.title|default('iCARE')|first }}<span class="text-[#febe10]">{{ content.title|default('iCARE')|slice(1) }}</span>
        </h1>
        <p class="mt-4 text-white/80 uppercase tracking-widest text-sm font-semibold max-w-2xl mx-auto">
            {{ content.subtitle|default("iTamaraw Center for Academic Resources and Enrichment") }}
        </p>
        <p class="mt-6 text-slate-300 text-lg max-w-2xl mx-auto font-light leading-relaxed">
            Committed to providing resources and enriching students' learning skills to enhance academic and communication abilities.
        </p>
        <div class="w-24 h-1 bg-[#febe10] mx-auto mt-8 rounded-full"></div>
    </div>
</section>

<!-- Main Services Section -->
<section class="py-20 bg-slate-50 relative overflow-hidden">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        
        <div class="text-center mb-16">
            <h2 class="text-4xl font-black text-slate-900 uppercase tracking-tight mb-4">Academic Services</h2>
            <div class="w-16 h-1 bg-feu-green mx-auto rounded-full"></div>
        </div>

        <!-- Dynamic Grid Engine mapping fields securely -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-20">
            {% for program in programs %}
                {% set themeColor = 'bg-feu-green text-white shadow-green-200' %}
                {% set hoverBorder = 'hover:border-feu-green' %}
                {% set accentBg = 'bg-green-50' %}

                {% if loop.index == 2 %}
                    {% set themeColor = 'bg-[#febe10] text-slate-900 shadow-yellow-200/50' %}
                    {% set hoverBorder = 'hover:border-feu-yellow' %}
                    {% set accentBg = 'bg-yellow-50' %}
                {% elseif loop.index == 3 %}
                    {% set themeColor = 'bg-blue-500 text-white shadow-blue-200' %}
                    {% set hoverBorder = 'hover:border-blue-500' %}
                    {% set accentBg = 'bg-blue-50' %}
                {% elseif loop.index == 4 %}
                    {% set themeColor = 'bg-purple-500 text-white shadow-purple-200' %}
                    {% set hoverBorder = 'hover:border-purple-500' %}
                    {% set accentBg = 'bg-purple-50' %}
                {% endif %}

                <div class="bg-white p-8 rounded-2xl shadow-sm border border-slate-100 group {{ hoverBorder }} transition-colors relative overflow-hidden">
                    <div class="absolute top-0 right-0 w-24 h-24 {{ accentBg }} rounded-bl-full -mr-4 -mt-4 transition-transform group-hover:scale-110"></div>
                    
                    <div class="w-14 h-14 {{ themeColor }} rounded-xl flex items-center justify-center mb-6 relative z-10 shadow-lg">
                        {% if program.iconChoice == 'chat' %}
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a1.994 1.994 0 01-1.414-.586m0 0L11 14h4a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2v4l.586-.586z"></path></svg>
                        {% elseif program.iconChoice == 'book' %}
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path></svg>
                        {% elseif program.iconChoice == 'clipboard' %}
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path></svg>
                        {% elseif program.iconChoice == 'users' %}
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path></svg>
                        {% elseif program.iconChoice == 'heart' %}
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path></svg>
                        {% else %}
                            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        {% endif %}
                    </div>
                    
                    <h3 class="text-2xl font-bold text-slate-900 mb-4 relative z-10">{{ program.title }}</h3>
                    <div class="text-slate-600 leading-relaxed relative z-10 text-sm">
                        {{ program.description|nl2br }}
                    </div>
                </div>
            {% endfor %}
        </div>

        <!-- Mental Health Support Block Context Area -->
        {% if content %}
        <div class="bg-white border border-slate-100 rounded-[2.5rem] p-10 md:p-14 relative overflow-hidden shadow-xl shadow-slate-200/50">
            <div class="absolute -right-20 -bottom-20 w-64 h-64 bg-green-50/50 rounded-full"></div>
            
            <div class="relative z-10 md:w-2/3">
                <span class="text-[#febe10] font-black tracking-widest uppercase text-xs mb-3 block">
                    {{ content.holisticWellnessTitle }}
                </span>
                <h2 class="text-3xl md:text-5xl font-black text-slate-900 mb-6 leading-tight">
                    {{ content.mentalHealthSupportTitle|split(' ')|first }} 
                    <span class="text-feu-green">{{ content.mentalHealthSupportTitle|split(' ')|slice(1)|join(' ') }}</span>
                </h2>
                <div class="text-slate-600 text-lg leading-relaxed mb-6 space-y-4">
                    {{ content.learningDescription|nl2br }}
                </div>
            </div>
        </div>
        {% endif %}
    </div>
</section>

<!-- Contact Block Area -->
{% if content %}
<section class="py-16 bg-white border-t border-slate-100">
    <div class="max-w-4xl mx-auto px-4 text-center">
        <h2 class="text-3xl font-black uppercase mb-6 text-slate-900">Reach Out to iCARE</h2>
        
        <div class="flex flex-col md:flex-row items-center justify-center gap-6 text-sm font-medium mt-8">
            <div class="flex items-center gap-3 bg-slate-50 border border-slate-200 px-6 py-3 rounded-full text-slate-700 hover:border-feu-green transition-colors">
                <svg class="w-5 h-5 text-feu-green" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
                <span>{{ content.floorLocation }}</span>
            </div>
            <div class="flex items-center gap-3 bg-slate-50 border border-slate-200 px-6 py-3 rounded-full text-slate-700 hover:border-feu-green transition-colors">
                <svg class="w-5 h-5 text-feu-green" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                <span>{{ content.emailAddress }}</span>
            </div>
        </div>
        
        <div class="mt-8">
            <a href="{{ content.fbLink }}" target="_blank" class="inline-flex items-center gap-2 text-[#0866FF] font-bold hover:underline">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.469h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.469h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
                Visit our official Facebook page
            </a>
        </div>
    </div>
</section>
{% endif %}
{% endblock %}