{% extends 'base.html.twig' %}

{% block title %}{{ content and content.metaTitle ? content.metaTitle : 'Student Activities and Development - FEU Institute of Technology' }}{% endblock %}

{% block body %}
<style>
    :root {
        --feu-green: #008348;
        --feu-yellow: #febe10;
    }
    .text-gradient {
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-image: linear-gradient(to right, var(--feu-green), #00b362);
    }
    .hover-lift {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .hover-lift:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 40px -10px rgba(0, 131, 72, 0.15);
    }
</style>

{# Hero Section #}
<section id="top" class="relative w-full h-[50vh] min-h-[400px] flex items-center justify-center overflow-hidden bg-slate-900">
    <div class="absolute inset-0 z-0">
        {% if content and content.heroImage %}
            <img src="{{ asset('images/studentservice/' ~ content.heroImage) }}" alt="Student Life" class="w-full h-full object-cover opacity-40 mix-blend-overlay">
        {% else %}
            <img src="/images/FACILITIES_TECH.jpg" alt="Student Life" class="w-full h-full object-cover opacity-40 mix-blend-overlay">
        {% endif %}
        <div class="absolute inset-0 bg-gradient-to-t from-slate-900 via-slate-900/60 to-transparent"></div>
    </div>

    <div class="relative z-10 text-center px-5 max-w-5xl mx-auto">
        <span class="text-[#febe10] font-bold tracking-[0.3em] uppercase text-xs mb-4 block">
            Student Journey
        </span>
        <h1 class="text-5xl md:text-7xl font-black text-white leading-tight uppercase tracking-tighter">
            {{ content ? content.heroTitle|raw : 'Student <span class="text-[#febe10]">Activities</span> & Development' }}
        </h1>
        <p class="mt-6 text-slate-300 text-lg max-w-3xl mx-auto font-light leading-relaxed">
            {{ content and content.heroSubtitle ? content.heroSubtitle : 'Developing students to become effective leaders, efficient followers, and active contributors to society.' }}
        </p>
        <div class="w-24 h-1 bg-[#febe10] mx-auto mt-8 rounded-full"></div>
    </div>
</section>

{# Vision & Mission #}
<section class="py-20 bg-slate-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12 lg:gap-24">
            <div class="bg-white p-10 rounded-3xl shadow-sm border border-slate-100 relative overflow-hidden group">
                <div class="absolute top-0 right-0 w-32 h-32 bg-green-50 rounded-bl-[100px] -z-10 group-hover:scale-110 transition-transform"></div>
                <div class="flex items-center gap-4 mb-6">
                    <div class="w-12 h-12 bg-feu-green text-white rounded-2xl flex items-center justify-center shadow-lg shadow-green-200">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path></svg>
                    </div>
                    <h2 class="text-3xl font-black text-slate-900 uppercase">Vision</h2>
                </div>
                <p class="text-slate-600 leading-relaxed text-lg">
                    {{ content and content.visionText ? content.visionText : 'A recognized premier provider of holistic development programs that produce effective, dynamic, transformational and innovative leaders of the society.' }}
                </p>
            </div>

            <div class="bg-white p-10 rounded-3xl shadow-sm border border-slate-100 relative overflow-hidden group">
                <div class="absolute top-0 right-0 w-32 h-32 bg-yellow-50 rounded-bl-[100px] -z-10 group-hover:scale-110 transition-transform"></div>
                <div class="flex items-center gap-4 mb-6">
                    <div class="w-12 h-12 bg-[#febe10] text-slate-900 rounded-2xl flex items-center justify-center shadow-lg shadow-yellow-200/50">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21l-7-5-7 5V5a2 2 0 012-2h10a2 2 0 012 2v16z"></path></svg>
                    </div>
                    <h2 class="text-3xl font-black text-slate-900 uppercase">Mission</h2>
                </div>
                <ul class="space-y-4 text-slate-600 text-lg">
                    {% if content and content.missionItems is not empty %}
                        {% for item in content.missionItems %}
                            <li class="flex items-start gap-3">
                                <svg class="w-6 h-6 text-feu-green flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                                {{ item }}
                            </li>
                        {% endfor %}
                    {% else %}
                        <li class="flex items-start gap-3">
                            <svg class="w-6 h-6 text-feu-green flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>
                            To mold innovative leaders guided by values of Fortitude, Excellence, and Uprightness.
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</section>

{# Dynamic Mantra Section #}
<section class="py-24 bg-white relative overflow-hidden">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="text-center mb-16">
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 uppercase tracking-tight mb-4">The <span class="text-gradient">Mantra</span></h2>
            <div class="w-24 h-1 bg-slate-200 mx-auto rounded-full"></div>
        </div>

        <div class="space-y-16 max-w-5xl mx-auto">
            {# Pillar 1 #}
            <div class="flex flex-col md:flex-row gap-8 items-start">
                <div class="md:w-1/3 flex-shrink-0">
                    <div class="text-6xl md:text-8xl font-black text-slate-200/50 tracking-tighter uppercase relative select-none">
                        {{ content and content.titleOne ? content.titleOne : 'Serve' }}
                        <h3 class="absolute inset-0 flex items-center text-4xl md:text-5xl font-black text-feu-green ml-6 tracking-wide drop-shadow-[0_2px_8px_rgba(15,23,42,0.15)]">
                            {{ content and content.titleOne ? content.titleOne : 'Serve' }}
                        </h3>
                    </div>
                </div>
                <div class="md:w-2/3 pt-4 md:pt-8 border-t border-slate-100 md:border-none">
                    <p class="text-slate-600 text-lg leading-relaxed">
                        {{ content and content.titleOneText ? content.titleOneText : 'Programs of SADU are geared toward creating a community of leaders who are passionate, proactive, innovative, and ever ready to be of service to others.' }}
                    </p>
                </div>
            </div>

            {# Pillar 2 #}
            <div class="flex flex-col md:flex-row gap-8 items-start">
                <div class="md:w-1/3 flex-shrink-0">
                    <div class="text-6xl md:text-8xl font-black text-slate-200/50 tracking-tighter uppercase relative select-none">
                        {{ content and content.titleTwo ? content.titleTwo : 'Lead' }}
                        <h3 class="absolute inset-0 flex items-center text-4xl md:text-5xl font-black text-feu-yellow ml-6 tracking-wide drop-shadow-[0_2px_8px_rgba(15,23,42,0.20)]">
                            {{ content and content.titleTwo ? content.titleTwo : 'Lead' }}
                        </h3>
                    </div>
                </div>
                <div class="md:w-2/3 pt-4 md:pt-8 border-t border-slate-100 md:border-none">
                    <p class="text-slate-600 text-lg leading-relaxed">
                        {{ content and content.titleTwoText ? content.titleTwoText : 'Leaders exhibit a unique blend of charisma, vision, and character traits that attract people.' }}
                    </p>
                </div>
            </div>

            {# Pillar 3 #}
            <div class="flex flex-col md:flex-row gap-8 items-start">
                <div class="md:w-1/3 flex-shrink-0">
                    <div class="text-6xl md:text-8xl font-black text-slate-200/50 tracking-tighter uppercase relative select-none">
                        {{ content and content.titleThree ? content.titleThree : 'Excel' }}
                        <h3 class="absolute inset-0 flex items-center text-4xl md:text-5xl font-black text-feu-green ml-8 tracking-wide drop-shadow-[0_2px_8px_rgba(15,23,42,0.15)]">
                            {{ content and content.titleThree ? content.titleThree : 'Excel' }}
                        </h3>
                    </div>
                </div>
                <div class="md:w-2/3 pt-4 md:pt-8 border-t border-slate-100 md:border-none">
                    <p class="text-slate-600 text-lg leading-relaxed">
                        {{ content and content.titleThreeText ? content.titleThreeText : 'FEU TECH student leaders are expected to imbibe excellence in their personal lives.' }}
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>

{# Features Bullet Blocks #}
<section class="py-20 bg-slate-900 relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
            
            <div class="bg-white/5 border border-white/10 p-10 rounded-3xl hover-lift">
                <h3 class="text-2xl font-bold text-white mb-6 uppercase tracking-wider flex items-center gap-3">
                    <span class="w-3 h-3 rounded-full bg-feu-yellow"></span>
                    Student Development
                </h3>
                <p class="text-slate-400 mb-8 line-clamp-3">
                    {{ content and content.devDescription ? content.devDescription : 'Creates programs that encourage formation in social, spiritual, cultural, technological, and recreational aspects.' }}
                </p>
                <ul class="space-y-4 text-slate-300">
                    {% set dev_items = content and content.devBullets is not empty ? content.devBullets : ['Holistic Development', 'Leadership Training Seminar', 'Efficiency Training', 'Personality Enhancement', 'Athletics'] %}
                    {% for bullet in dev_items %}
                        <li class="flex items-center gap-3"><svg class="w-5 h-5 text-feu-yellow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg> {{ bullet }}</li>
                    {% endfor %}
                </ul>
            </div>

            <div class="bg-white/5 border border-white/10 p-10 rounded-3xl hover-lift">
                <h3 class="text-2xl font-bold text-white mb-6 uppercase tracking-wider flex items-center gap-3">
                    <span class="w-3 h-3 rounded-full bg-feu-yellow"></span>
                    Student Activities
                </h3>
                <p class="text-slate-400 mb-8 line-clamp-3">
                    {{ content and content.actDescription ? content.actDescription : 'Supervises and regulates the operations of Recognized Student Organizations and Special Interest Groups.' }}
                </p>
                <ul class="space-y-4 text-slate-300">
                    {% set act_items = content and content.actBullets is not empty ? content.actBullets : ['Accreditation of Organizations', 'Recognition of Organization Advisers', 'Approval of Student Activities', 'Social Media Postings Approval', 'Contingency Selection for Conventions'] %}
                    {% for bullet in act_items %}
                        <li class="flex items-center gap-3"><svg class="w-5 h-5 text-feu-yellow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg> {{ bullet }}</li>
                    {% endfor %}
                </ul>
            </div>

        </div>
    </div>
</section>

{# Contact Section #}
<section class="py-16 bg-feu-green text-white relative overflow-hidden">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center">
        <h2 class="text-3xl font-black uppercase mb-8 tracking-wider">Contact Student Affairs</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
            <div class="p-6 bg-white/10 rounded-2xl backdrop-blur-sm border border-white/20 hover:bg-white/20 transition-colors">
                <svg class="w-8 h-8 mx-auto mb-4 text-feu-yellow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path></svg>
                <p class="font-bold">Location</p>
                <p class="text-sm text-green-100 mt-2">{{ content and content.contactRoom ? content.contactRoom : 'Room 1501' }}</p>
            </div>
            <div class="p-6 bg-white/10 rounded-2xl backdrop-blur-sm border border-white/20 hover:bg-white/20 transition-colors">
                <svg class="w-8 h-8 mx-auto mb-4 text-feu-yellow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path></svg>
                <p class="font-bold">Phone</p>
                <p class="text-sm text-green-100 mt-2">{{ content and content.contactPhone ? content.contactPhone : '(02) 8281 8888 Local 128' }}</p>
            </div>
            <div class="p-6 bg-white/10 rounded-2xl backdrop-blur-sm border border-white/20 hover:bg-white/20 transition-colors">
                <svg class="w-8 h-8 mx-auto mb-4 text-feu-yellow" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path></svg>
                <p class="font-bold">Email</p>
                <a href="mailto:{{ content and content.contactEmail ? content.contactEmail : 'sadu@feutech.edu.ph' }}" class="text-sm text-green-100 mt-2 hover:text-white transition-colors">
                    {{ content and content.contactEmail ? content.contactEmail : 'sadu@feutech.edu.ph' }}
                </a>
            </div>
        </div>
    </div>
</section>
{% endblock %}